# 具备流量审计的企业代理服务器 — 项目说明


## 1. 项目简介

本项目是一个用 Go 实现的企业级中间人（MITM）代理服务器，核心能力包括：

- **代理转发**：同时支持明文 HTTP 转发与 HTTPS（CONNECT 隧道 + 中间人解密）。
- **按黑白名单 / 特征精准拦截**：支持按端口、域名、IP 三类黑名单拦截，也支持按"用户 / 角色 / 源 IP / 域名 / 端口 / 方法"等特征的访问控制规则（ACL）。
- **日志记录（流量审计）**：记录每一次请求的完整审计信息（时间、用户、目标、状态、大小、耗时、头部、脱敏后的请求体），并提供管理端查询、导出（JSON / HAR）。
- **按角色分配权限**：代理用户体系 + 角色 + ACL 规则，不同角色对同一目标可以有不同放行/拦截策略。
- **静态资源本地缓存**：对可缓存响应（如 jpg/png/webp/css/js）在本地按 TTL 缓存，命中直接返回，减少上游流量。

项目参考开源仓库 [Welfordian/mitm-proxy](https://github.com/Welfordian/mitm-proxy) 的架构

---

## 2. 功能说明

### 2.1 代理转发（核心）

- 入口 `ServeHTTP`：`CONNECT` 请求走 HTTPS 隧道/中间人分支，其余按明文 HTTP 转发（见 `internal/proxy/proxy.go`、`http_plain.go`、`connect.go`）。
- 明文 HTTP：先做访问控制 → 黑白名单拦截 → 缓存命中检查 → 直连上游转发 → 响应回写。
- HTTPS：`CONNECT` 建立隧道后，用本地 CA 为域名现场签发叶子证书，做 MITM 解密后按明文 HTTP 同样处理（`mitm_http11.go`、`mitm_http2.go`）。
- 支持 HTTP/1.1 与 HTTP/2 两种明文代理请求；WebSocket（ws://）透明隧道保留（`websocket.go`）。

### 2.2 按黑白名单 / 特征拦截

- **黑名单**（`internal/policy/blocklist.go` + 管理端 `Blocks` 面板）：
  - 端口黑名单（如 25/445/3389）、域名黑名单（支持 `*.example.com` 通配）、IP 黑名单（支持 CIDR）。
  - 拦截后返回可配置状态码（默认 403）与拦截说明页。
- **特征级 ACL**（`internal/access/access.go` + 管理端 `Access Control` 面板）：
  - 规则字段：用户列表、角色列表、源 IP（支持 CIDR）、域名模式（支持 `*.` 通配）、端口（支持范围 `1000-2000`）、方法。
  - 规则按创建顺序匹配，命中即生效（`action: allow | deny`）；未命中走默认策略（`default_action`）。
  - 代理用户密码使用 bcrypt 哈希存储，支持启用/禁用。

### 2.3 日志记录（流量审计）

- 每次请求记录：时间、请求 ID、用户、方法、URL、协议、状态码、响应大小、耗时、是否缓存命中（`internal/store` 的 `traffic_flows` 表）。
- 详细审计：请求/响应头、脱敏后的请求体（`internal/redact`），脱敏字段可在配置中指定。
- 管理端 API：`/api/traffic`（分页列表）、`/api/traffic/{id}`（详情）、`/api/traffic/export`（JSON / HAR 导出）、`/api/traffic/stats`（统计）、`/api/traffic/stream`（SSE 实时推送）。
- 管理操作审计：管理端自身的敏感操作（登录、改配置、增删用户/规则、清缓存等）写入 `/api/audit`。

### 2.4 按角色分配权限

- 代理用户（`/api/proxy-auth/users`）：用户名、密码（bcrypt）、角色、启用状态。
- 角色（`/api/proxy-acl/rules` 中的 `Roles` 字段）：规则可按角色限定，例如"admin 角色放行 example.com，guest 角色拦截 example.com"。
- 客户端通过 `Proxy-Authorization: Basic ...` 认证后，代理按用户 + 角色 + 请求特征匹配 ACL 决定放行/拦截。
- 管理端提供 `Matcher Test`（/api/proxy-acl/test）可预先验证"某个用户访问某个目标"是否放行。

### 2.5 静态资源本地缓存

- 可缓存条件：GET 等可缓存方法 + 扩展名命中（默认 jpg/png/webp/css/js）+ 域名包含/排除规则。
- 缓存存放在本地磁盘目录（`cache.directory`），带 TTL（默认 3600 秒）。
- 命中时直接回包并附带 `Via` / 自定义 UID 头标识，**条目的命中次数自动 +1**；未命中时转发并把响应写入缓存（命中数从 0 起计）。
- 管理端 `Cache` 面板可查看缓存条目（含 URL / 状态 / 过期时间 / 大小 / **命中次数**）、按资源 URL 查看、清空缓存。

### 2.6 管理端（Web 控制台）

- 独立管理服务（`--admin-addr`，默认 127.0.0.1:9090），支持 Bearer Token 认证（`--admin-token`，可只读 Token `--admin-read-token`）。
- 内嵌 Web UI（React 构建产物 embed 进二进制，界面为中文）：总览 / 流量 / 证书 / 访问控制 / 黑名单 / 部署 / 缓存 / 设置 / 审计日志 九个面板。
- 全部管理功能同时提供 REST API（见第 4.4 节），便于脚本化调用。

---

## 3. 系统架构

### 3.1 模块划分

```
main.go                         入口：解析命令行/配置文件，组装并启动各模块
internal/
├── config/    配置加载、校验、热更新（config.json + 命令行覆盖）
├── proxy/     代理核心：HTTP 转发、CONNECT/MITM、WebSocket、缓存命中、审计事件发布
├── ca/        本地 CA：生成 CA 证书、按域名签发叶子证书
├── access/    访问控制：代理用户认证（Basic）、ACL 规则匹配、拒绝页
├── policy/    黑名单引擎：端口 / 域名 / IP 匹配（与配置无关的纯规则）
├── cache/     磁盘缓存：写入、命中、TTL、按域名/扩展名过滤
├── redact/    审计脱敏：请求/响应头与请求体的敏感信息脱敏
├── store/     SQLite 存储：流量、审计、用户、ACL、缓存条目、证书、部署状态
├── events/    事件总线：代理发布流量/缓存/证书事件，管理端订阅落库与推送
├── admin/     管理服务：REST API、SSE 推送、内嵌 Web UI（React）
│   ├── api/   管理端 API 路由
│   ├── auth/  Token 认证（管理端）
│   └── ui/    内嵌 React 前端（embed）
└── deployments/  部署信息与运行控制（reload / restart）
```

### 3.2 一次请求的处理流程（明文 HTTP）

```
客户端 ──HTTP──> 代理 ServeHTTP
  1. 访问控制（internal/access）
     - 未开启代理认证：直接放行
     - 开启认证：校验 Proxy-Authorization（用户名/密码/启用状态）
       → 加载用户角色 → 按顺序匹配 ACL 规则 → allow/deny
     - 拒绝：返回 403 或 407 拦截页（含原因、命中规则、用户、目标）
  2. 黑白名单拦截（internal/policy）
     - 端口 / 域名 / IP 命中黑名单 → 返回 403 拦截页
  3. 缓存命中检查（internal/cache）
     - 可缓存且命中 → 直接回包（附 Via / 缓存 UID 头，命中次数 +1），发布 cache.hit 事件
  4. 直连上游转发（internal/proxy/transport.go）
     - 转发请求 → 读取响应
     - 可缓存则写入缓存，发布 cache.miss / traffic.response.completed 事件
  5. 审计（internal/store + internal/redact）
     - 记录流量详情（头、脱敏体、状态、耗时、大小），可实时推送管理端
```

### 3.3 HTTPS / MITM 流程

```
客户端 ──CONNECT host:443──> 代理
  1. 建隧道（internal/proxy/connect.go），此时校验一次代理认证（Proxy-Authorization）
  2. 用本地 CA 为该域名签发叶子证书（internal/ca + proxy.getCertForHost，带缓存）
  3. 与客户端做 TLS 握手（呈现叶子证书），与上游做 TLS 连接
  4. 解密后按明文 HTTP 流程处理（访问控制 → 拦截 → 缓存 → 转发 → 审计）；
     内层请求不再重复要求凭据（CONNECT 已认证，只按用户名查启用状态并匹配 ACL）
  5. 客户端需信任 ca-cert.pem（管理端 Certificates 面板可查看/下载/导入/轮换 CA）
```

### 3.4 配置热更新

- `main.go` 监听 `config.json` 变化（`--watch-config`，默认开启），配置变更后调用 `Proxy.SetConfig` / 管理端同步更新，无需重启进程。
- 命令行参数优先级高于配置文件。

---

## 4. 使用方法

### 4.1 环境要求

- Go 1.21+（本项目在 Go 1.25 下开发验证）
- 无需外部数据库（SQLite 由纯 Go 驱动，数据落本地 `dashboard.db`）

### 4.2 构建与启动

```bash
# 构建（管理端 UI 已 embed，无需前端工具链）
go build -o enterprise-proxy.exe .

# 启动（默认读取 ./config.json）
./enterprise-proxy.exe

# 常用参数
./enterprise-proxy.exe --config ./config.json \
  --listen :8080 --admin-addr 127.0.0.1:9090 \
  --admin-token your-secret-token
```

启动后：

- 代理监听 `:8080`（HTTP 代理端口）
- 管理端 http://127.0.0.1:9090/admin/ （浏览器首次访问会提示输入 Token）
- 首次启动自动生成本地 CA（`ca-cert.pem` / `ca-key.pem`）

### 4.3 命令行参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `--config` | 空 | config.json 路径 |
| `--listen` | 配置项 | 代理监听地址（覆盖配置） |
| `--ca-cert` / `--ca-key` | 配置项 | 已有 CA 证书/私钥路径（覆盖配置） |
| `--mitm` | true | 启用 MITM 解密 |
| `--verbose` | false | 详细日志 |
| `--admin-enabled` | true | 启用管理服务 |
| `--admin-addr` | 127.0.0.1:9090 | 管理服务监听地址 |
| `--admin-token` | 自动生成 | 管理 Token（空则启动时自动生成并打印） |
| `--admin-read-token` | 空 | 只读 Token |
| `--admin-ui` | true | 内嵌 Web UI |
| `--admin-store` | dashboard.db | SQLite 数据文件 |
| `--watch-config` | true | 监听配置热更新 |

### 4.4 配置项（config.json）

| 配置段 | 关键字段 | 说明 |
| --- | --- | --- |
| 顶层 | `listen_addr` / `enable_mitm` / `verbose_logging` / `log_requests` | 监听地址、MITM、日志开关 |
| 顶层 | `excluded_domains` | 跳过代理的域名列表 |
| CA | `ca_cert_path` / `ca_key_path` / `ca_cert_output_path` / `ca_key_output_path` | CA 证书/私钥路径 |
| 管理 | `admin_enabled` / `admin_addr` / `admin_token` / `admin_read_token` / `admin_ui` / `admin_store` | 管理服务配置 |
| `cache` | `enabled` / `directory` / `include_domains` / `exclude_domains` / `include_extensions` / `exclude_extensions` / `ttl` | 静态资源本地缓存 |
| 黑名单 | `blocked_ports` / `blocked_domains` / `blocked_ips` / `block_action` / `block_response_status` | 端口/域名/IP 黑名单与拦截响应 |
| `traffic_capture` | `store_bodies` / `max_body_bytes` / `redact_bodies` / `store_headers` / `redacted_headers` / `store_cookies` / `redacted_cookies` | 流量审计存储与脱敏 |
| `proxy_auth` | `enabled` / `realm` / `require_auth_for_loopback` / `default_action` | 代理认证与默认策略 |

### 4.5 使用代理

```bash
# curl 走代理（明文 HTTP）
curl -x http://127.0.0.1:8080 http://example.com/

# curl 走代理 + 代理认证（用户名/密码，Basic）
curl -x http://127.0.0.1:8080 -U alice:secret http://example.com/

# HTTPS（需信任 ca-cert.pem）
curl -x http://127.0.0.1:8080 --cacert ca-cert.pem https://example.com/

# 浏览器：将系统代理设置为 127.0.0.1:8080，并导入 ca-cert.pem 到受信任根证书
```

### 4.6 管理端 API（节选）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` / `/api/version` | 健康检查 / 版本 |
| GET | `/api/traffic` / `/api/traffic/{id}` | 流量列表 / 详情 |
| GET | `/api/traffic/export` / `/api/traffic/{id}/export` | 导出（JSON / HAR） |
| GET | `/api/traffic/stats` / `/api/traffic/stream` | 统计 / SSE 实时流 |
| GET | `/api/audit` | 管理操作审计日志 |
| GET/POST/DELETE | `/api/proxy-auth/users` | 代理用户管理 |
| GET/POST/DELETE | `/api/proxy-acl/rules` | ACL 规则管理 |
| POST | `/api/proxy-acl/test` | ACL 预演（用户 → 目标是否放行） |
| GET/POST/DELETE | `/api/blocks/ports` `/api/blocks/domains` `/api/blocks/ips` | 黑名单管理 |
| GET/POST | `/api/blocks/test` | 黑名单匹配预演 |
| GET/DELETE | `/api/cache` / `/api/cache/purge` / `/api/cache/resource` | 缓存查看 / 清理 |
| GET/POST | `/api/certificates/ca` `/api/certificates/ca/download` `/api/certificates/ca/import` `/api/certificates/ca/rotate` | CA 管理 |
| GET | `/api/deployments/current` `/api/logs` | 运行状态 / 日志 |
| GET/PUT | `/api/settings` | 运行配置读取 / 更新 |

所有 `/api/*` 请求需带管理 Token：`Authorization: Bearer <token>`。

---

## 5. 测试与验证

### 5.1 单元测试

```bash
go test ./...
```

覆盖：access（ACL 匹配）、admin（API handler）、ca（证书签发）、cache（缓存读写/TTL）、events（事件总线）、policy（黑名单匹配）、proxy（转发/拦截/MITM）、store（存储）。

### 5.2 端到端冒烟（集成测试）

曾使用仓库根目录的 `smoke_full.py`（Python 3，仅标准库）自动完成 17 项端到端验证；该脚本已随"干净交付版本"移除，验证结果见《测试文档.md》。脚本当时的验证链路为：

1. 启动本地上游测试服务器（127.0.0.1:19080）；
2. 以 `--admin-addr 127.0.0.1:19090 --admin-token smoketoken123` 启动代理；
3. 依次验证 17 项：健康检查、创建 admin/guest 用户（角色落库）、创建 allow/deny ACL 规则、ACL 预演（admin 放行 / guest 拦截 example.com）、角色持久化、域名/端口/IP 黑名单增删查、HTTP 经代理转发、审计日志非空、流量列表可读。

结果：`SMOKE RESULT: 17/17 passed`。逐步手工复现方式见《项目使用录制讲解指南.md》。

### 5.3 功能验证清单（对应题目需求）

| 题目需求 | 验证方式 | 结果 |
| --- | --- | --- |
| 代理转发（HTTP/HTTPS） | curl 经 8080 访问上游 | 通过 |
| 黑白名单 / 特征拦截 | 加入黑名单域名/端口/IP 后访问被 403；ACL 按角色拦截 | 通过 |
| 日志记录 | `/api/traffic` 与 `/api/audit` 可见完整记录 | 通过 |
| 按角色分配权限 | admin 放行 / guest 拦截同一目标 | 通过 |
| 静态资源本地缓存 | 重复请求命中缓存（Via 头 + cache.hit 事件） | 通过 |


## 6. 已知边界

- 本仓库为课程项目交付，仅用于授权网络的代理审计实验，请勿用于未授权环境。
- WebSocket 隧道为透明转发（不做内容审计）；审计重点是 HTTP/HTTPS 流量。
- 缓存为简化磁盘缓存：不处理 Cache-Control 语义，仅按扩展名 + TTL 判定。
